apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketing-service
  namespace: transport-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ticketing-service
  template:
    metadata:
      labels:
        app: ticketing-service
    spec:
      containers:
      - name: ticketing-service
        image: transport/ticketing-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8094
        env:
        - name: CONFIG_SERVER_URL
          valueFrom:
            configMapKeyRef:
              name: transport-config
              key: CONFIG_SERVER_URL
        - name: EUREKA_DEFAULT_ZONE
          valueFrom:
            configMapKeyRef:
              name: transport-config
              key: EUREKA_DEFAULT_ZONE
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            configMapKeyRef:
              name: database-config
              key: TICKETING_DB_URL
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            configMapKeyRef:
              name: database-config
              key: TICKETING_DB_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: transport-secrets
              key: TICKETING_DB_PASSWORD
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: transport-config
              key: KAFKA_BOOTSTRAP_SERVERS
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/tickets/health
            port: 8094
          initialDelaySeconds: 120
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/tickets/health
            port: 8094
          initialDelaySeconds: 90
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: ticketing-service
  namespace: transport-system
spec:
  selector:
    app: ticketing-service
  ports:
  - port: 8094
    targetPort: 8094
  type: ClusterIP
